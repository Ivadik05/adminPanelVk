export type marketType = {
  id: string,
  ownerId: string,
  title: string,
  description: string,
  price: string,
  category: Object,
  date: Date,
  photo: string
}

export type commentsType = {
  id: string,
  text: string
}
